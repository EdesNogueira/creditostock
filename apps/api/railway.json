{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g pnpm && pnpm install && pnpm --filter api run build"
  },
  "deploy": {
    "startCommand": "cd apps/api && npx prisma migrate deploy && cd ../.. && node apps/api/dist/src/main",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
